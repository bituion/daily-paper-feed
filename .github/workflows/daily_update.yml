name: LEGACY (disabled) - use update-pages.yml

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - name: This workflow is disabled
        run: |
          echo "This is a legacy workflow and is intentionally disabled."
          echo "Use '.github/workflows/update-pages.yml' (Update papers and Pages)."
